# HttpPlugin for PolyCore
# Simple HTTP Server for a Web-based client

config-version: 1

# The port to listen on
port: 5678
# The host to bind to (use 0.0.0.0 to listen on all interfaces)
host: 0.0.0.0